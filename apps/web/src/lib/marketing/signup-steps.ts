/** Shared step labels for `<WizardStepper>` across the 6-step signup flow (FRONTEND_SPEC.md §4) — one source instead of copy-pasted in every step's client component. */
export const SIGNUP_STEPS: string[] = [
  "Business info",
  "Plan",
  "Account",
  "Payment",
  "Provisioning",
  "Phone setup",
];
