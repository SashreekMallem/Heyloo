# Cross-cluster fix requests

One bullet per request: target file + exact change + requesting cluster.
The integrator applies these — do not edit another cluster's ownership
paths directly.

FIX-1 (this pass) verified every bullet previously filed here against the
current tree and removed everything confirmed applied (see
`docs/BUILD_NOTES.md`'s FIX-1 section for the per-bullet confirmation
trail). What remains below is genuinely still open.

- **Target:** `docs/DEPLOY.md` §5 (Counsel checklist) or wherever BIPA retention-window policy is tracked outside cluster A's ownership (`docs/DEPLOY.md` itself is cluster A's, but the underlying `tenants.retention_days` default value/UI is not).
  **Change:** once counsel picks a retention-window default (§5's own open item, "Recording retention default (30 vs 90 days) pending BIPA counsel input"), the `tenants.retention_days` column already exists (`supabase/migrations/20260907130100_tenancy.sql`, `default 30`) — no schema change needed, just confirm the default value counsel picks matches the column default, or file a follow-up migration to change it if not. Flagging here only so whichever cluster owns the tenant-provisioning/dashboard-settings UI knows the column is already wired and awaiting a confirmed default, not a build gap.
  **Requesting cluster:** A (database hardening) — informational, no urgent action; blocked on an external (legal) decision, not code.

- **Target:** `supabase/migrations/<timestamp>_tenant_usage_alert_prefs.sql` (new, additive) — or, alternatively, confirm in `docs/BUILD_NOTES.md`/`docs/spec/FRONTEND_SPEC.md` that the platform-wide default is the intended final design and no per-tenant override should exist.
  **Change:** `packages/canonical-types/src/schemas/usage-alert-config.ts`'s `usageAlertConfigSchema` (`alert_80_enabled`, `alert_100_enabled`, `hard_cap_enabled`, `hard_cap_minutes`) implies a PER-TENANT setting, but no such columns exist anywhere — only the platform-wide `platform_settings.usage_alert_thresholds` key and the already-admin-settable `tenants.usage_hard_cap_minutes`. `apps/web`'s Billing page shows the real platform-wide thresholds + real hard-cap state as read-only text rather than a non-persisting toggle (CLAUDE.md: no decorative controls) — `docs/BUILD_NOTES.md`'s Cluster C entry treats this as the current final design pending an explicit product decision, not a bug; still no migration has actually been written, so leaving this filed until someone makes that product call.
  **Requesting cluster:** C (realtime + dashboard/admin truthfulness) — informational, product decision pending, not a code gap in the shipped behavior.

- **Target:** `supabase/functions/webhooks-pos/handler.ts` (or wherever the Airtable webhook/poll path would live) — outside this cluster's exact task brief but flagging since it touches `worker-adapter-push`'s Airtable pusher.
  **Change:** `worker-adapter-push/handler.ts`'s `ADAPTER_PUSHERS` map has a real `pushToAirtable` branch (one-way push only, `_shared/providers/airtable.ts`). No poll-based pull-back (two-way sync) was added for Airtable — only Square/ezyVet/Shopmonkey/Google Calendar have `pollAdapterChanges` support; flagging as a known gap, not silently claimed as done.
  **Requesting cluster:** E (voice tools/workers/adapter security) — informational/dependency note, not a blocking ask.

- **Target:** `supabase/functions/_shared/queue.ts` (`QUEUE_NAMES.outreachSend`/`OutreachSendQueueMsg`) + a new, additive migration (never edit `20260910093000_queues_and_scheduled_jobs.sql` after it's applied) dropping `outreach_send_queue`/`outreach_send_queue_dlq` via `pgmq.drop_queue('<name>')` (guarded by the same `pg_extension`/`pgmq.list_queues()` existence checks that migration already uses) — or, if the outreach-owning cluster's design intends to migrate the send path onto pgmq, a new `worker-outreach-send` function + `cron.schedule`/`fn_cron_upsert` entry that actually drains it instead.
  **Change:** DB-B3's migration creates `outreach_send_queue`/`outreach_send_queue_dlq` (matching BACKEND_SPEC §9's literal 4-queue table), but the real T8 outreach-send pipeline (`job-outreach-personalize-collect/handler.ts`) writes directly to a `send_events` table and calls Smartlead's API directly — still zero call sites enqueue to or read `outreach_send_queue` anywhere in `supabase/functions/**`. This queue+DLQ pair is decorative pgmq infrastructure today. Still unresolved as of FIX-1 (this pass did not own `supabase/functions/job-outreach-personalize{,-collect}` or the outreach-send design and did not redesign it per CLAUDE.md Rule 4).
  **Requesting cluster:** whichever cluster owns `supabase/functions/job-outreach-personalize{,-collect}`/the outreach-send design.

- **Target:** `supabase/migrations/20260907130100_tenancy.sql`'s `tenants` table (new, additive column) — informational, not blocking.
  **Change:** `job-offboarding/handler.ts` needs "when did this tenant most recently enter `paused`/`canceled` status" to gate its 30-day guaranteed-port-out-SLA window, but no such timestamp exists — it uses `tenants.updated_at` as a conservative proxy (documented in-file: any later touch to a canceled/paused tenant's row only ever DELAYS release, never releases early, so this is safe-direction, not a correctness bug). A real `tenants.canceled_at timestamptz`/`paused_at timestamptz` pair (set by whichever code path flips `tenants.status`) would let this job key off the actual event instead of a proxy.
  **Requesting cluster:** F (referral payout webhook, churn/retention, integrations UI) — informational, current behavior is safe, not urgent.

- **Target:** `supabase/functions/voice-inbound/dynamic-variables.ts` + `handler.ts` (whichever cluster owns the per-call dynamic-variable assembly, most recently Cluster B — see `docs/BUILD_NOTES.md`'s Cluster B entry).
  **Change:** add a `transfer_number` dynamic variable, sourced from `agent_configs.transfer_number` (the plain top-level E.164 column, `supabase/migrations/20260907130300_agent_templates.sql:40` — NOT the `dynamic_variable_overrides` jsonb blob the rest of `dynamic-variables.ts` resolves from) to the per-call `retell_llm_dynamic_variables`/`call_inbound.dynamic_variables` object `handler.ts` returns. Cluster A's compiler (this pass, GAP_REGISTER §1.4 item 4 "transfer_call native wiring") now compiles every template's `transfer_call` tool/node to Retell's native transfer mechanism with `transfer_destination: {type:"predefined", number:"{{transfer_number}}"}` (`packages/adapters/retell/src/compiler/{conversation-flow,multi-prompt,single-prompt}.ts`) — matching BACKEND_SPEC §7.2.8's "resolved only from `agent_configs.transfer_number`, never a runtime tool argument" via the SAME server-side dynamic-variable mechanism the rest of this system already uses for tenant config, rather than baking the number in at agent-publish time (which would require plumbing tenant config through `CreateOrUpdateAgentInput`/`RetellProvider.createOrUpdateAgent`, itself outside this cluster's file ownership — `packages/canonical-types/src/voice-provider.ts`'s `zAgentDynamicVariables.transfer_number` field is already added for this). Until this lands, a tenant's transfer_call compiles correctly but the literal placeholder `{{transfer_number}}` is either spoken/dialed unresolved or (per Retell's own handling of an unset dynamic variable) left blank — no regression from before (transfer_call previously didn't work at all, custom-webhook-only), but this is the remaining piece to make it actually functional end-to-end.
  **Requesting cluster:** A (conversation engine) — blocking for `transfer_call` to work end-to-end, not blocking for anything already shipped.

- **Target:** `packages/adapters/retell/src/provider.ts` (not in Cluster A's file ownership for this pass).
  **Change:** (1) add `createOutboundCall(input): Promise<CreateOutboundCallResult>` to the `RetellProvider` class, delegating to the new `createRetellOutboundCall(this.client, input)` in `packages/adapters/retell/src/outbound.ts` (GAP_REGISTER Cluster A item 6 — complete, independently-tested Retell implementation already written, just not wired onto the class); (2) set `supportsOutboundCalls: true` on `RETELL_CAPABILITIES` (`provider.ts`). Both `VoiceProvider.createOutboundCall` and `ProviderCapabilities.supportsOutboundCalls` (`packages/canonical-types/src/voice-provider.ts`) were added as OPTIONAL specifically so this file didn't need to change for the rest of Cluster A's work to typecheck — this request is the follow-up to actually make outbound calling reachable through the `RetellProvider` instance Cluster G's saga code will hold. See `docs/VERIFY.md` VERIFY-10 for the one open item on `outbound.ts` itself (response-shape confidence, not blocking).
  **Requesting cluster:** A (conversation engine) — needed by Cluster G (outbound calling consumer, per this pass's task brief).

- ~~**Target:** `packages/templates/src/verticals/veterinary.ts` and `dental.ts`~~ — **APPLIED** (this pass, Cluster F): `veterinary.ts`'s `emergency_referral` state and `dental.ts`'s `pain_triage` state now each declare `extraction: [{field: "emergency_detected", type: "boolean", ...}, {field: "urgency_flag", type: "enum", enum_values: [...], ...}]`, wired to Cluster A's already-built lowering pass with no compiler change needed. Test coverage: `packages/templates/src/red-team/structural.test.ts` (full suite green, `pnpm --filter @heyloo/templates test`).
  **SUPERSEDED (2026-09-10 REPAIR pass):** the `urgency_flag` half of the above was removed from both files. It was never actually read by name (`voice-events/handler.ts`'s `handleCallAnalyzed` only ever reads `emergency_detected`, confirmed still true), and vet's/dental's own `urgency_flag` enum vocabularies differ from each other ("emergency"/"routine" vs "same_day"/"routine") and from the `emergency_detected`-only field this pass added to the shared `safetyEmergencyState()` (now used for the same purpose by motel/restaurant/real_estate/generic/legal too) — since the compiler's `post_call_analysis_data` dedupes by field NAME across a whole template (first declaration in `states[]` wins), a second differently-voculated `urgency_flag` would just be silently dropped wherever a template declares its own. `emergency_detected` is now documented (inline in `voice-events/handler.ts`) as the one authoritative signal `call_logs.urgency_flag` is derived from. Full detail in `docs/BUILD_NOTES.md`'s 2026-09-10 REPAIR entry.

- ~~**Target:** `packages/templates/src/verticals/generic.ts`~~ — **ACKNOWLEDGED, not trimmed** (this pass, Cluster F): decided to KEEP `single_prompt` and ADD MORE (not fewer) tools — `manageBookingState()` + `update_booking`/`cancel_booking` (GAP_REGISTER §1.12 BLOCKER: generic had no reschedule/cancel path at all) — accepting a larger overage rather than trimming, since a caller being unable to reschedule/cancel is a harder functional gap than a soft word-budget warning (the budget test remains soft-warn only, confirmed still non-blocking: `pnpm --filter @heyloo/adapter-retell test` — 138/138 green after this pass's `packages/templates` changes). Full rationale (including why generic was NOT moved to a costlier compile tier the way `real_estate` was, for the same budget pressure) logged in `docs/BUILD_NOTES.md`'s Cluster F entry and in `generic.ts`'s own docstring.

- **Target:** `apps/web/src/app/api/admin/[...path]/route.ts` — this proxy requires the CALLING request's own session cookie to carry `platform_admin` (`if (!claims.platform_admin) return 403`), and `admin/handler.ts`'s `impersonate/edit-mode` route additionally requires the forwarded bearer JWT's own claims to pass `isAal2(ctx.claims)` and carry a resolvable `ctx.adminUserId` from that SAME token's `sub`/claims.
  **Change:** the "Enable edits" toggle (`use-impersonation-banner.tsx`'s `onToggleEdit`) calls this proxy from the IMPERSONATED tab's `ImpersonationBanner`. `@supabase/ssr` stores the session in one per-domain cookie, not per-tab, so opening the admin's impersonation magic link in a new tab overwrites that cookie for the whole browser — a request made from inside the impersonated tab afterward authenticates as the TENANT OWNER's session, not the admin's, and both this proxy's `platform_admin` check AND the edge function's own AAL2/`ctx.adminUserId` check reject it (this affects `impersonate-end` too, pre-existing). FIX-1 confirmed this is still unresolved and did NOT attempt it: a real fix needs BOTH the proxy AND `admin/handler.ts`'s own JWT/claims parsing to accept a second, `impersonated_by`-claim-based trust path (only stampable by `custom_access_token_hook` from a real, currently-active `impersonation_sessions` row, so it can't be forged by an ordinary tenant login) as an alternative identity/AAL2 source — a genuine, security-sensitive auth-model change spanning two services, not a small patch (CLAUDE.md Rule 4: flag, don't redesign under a different task's scope). The toggle itself still degrades honestly (only flips displayed `editMode` on an actual 200, shows an error toast otherwise) rather than faking success while this is unresolved.
  **Requesting cluster:** whoever owns `apps/web/src/app/api/admin/**` + `supabase/functions/admin/**` jointly (the fix cannot land in only one of the two).

- ~~**Target:** `packages/canonical-types/src/agent-template.ts`'s `zRestaurantOverrides`~~ — **APPLIED** (this pass, Cluster A): added `menu_text`/`tax_rate_bps` to `zRestaurantOverrides`; `prep_time_minutes` was already inherited from `zBaseDynamicVariableOverrides`, confirmed. Test coverage extended in `packages/canonical-types/src/agent-template.test.ts` ("restaurant accepts menu_text + tax_rate_bps", "rejects a tax_rate_bps outside 0-10000"). Full run: `pnpm --filter @heyloo/canonical-types test` green.

- **Target:** `packages/templates/src/shared/tools.ts`'s `checkAvailabilityTool()` (Cluster CD's file, per the current build-wave ownership map — Cluster F does not own it).
  **Change:** add a `room_type: { type: "string" }` property to `check_availability`'s model-facing JSON-Schema `parameters.properties` (mirroring `zCheckAvailabilityRequest.room_type` in `packages/canonical-types/src/tools.ts`, and the SQL filter `supabase/functions/voice-tools/tools/check_availability.ts` already applies — GAP_REGISTER.md §2 Motel item 2). The runtime schema and the SQL filter are both already correct; only the tool-definition JSON-Schema the model actually sees is missing the field, so the model currently has no declared slot to put a chosen room type in. `packages/templates/src/verticals/motel.ts`'s `check_time` state (this pass, Cluster F) already instructs passing `room_type`, ready for when this lands — see that file's own top-of-file NOTE.
  **Requesting cluster:** F (per-vertical template authoring) — blocking for motel's capacity-aware room-type availability to actually reach the SQL filter; not a regression (previously no template referenced `room_type` at all).

- **Target:** `packages/templates/src/shared/tools.ts`'s `createOrderTool()` (Cluster CD's file).
  **Change:** add `allergies: { type: "array", items: { type: "string" } }` and `special_instructions: { type: "string" }` properties to `create_order`'s model-facing JSON-Schema `parameters.properties` (mirroring `zCreateOrderRequest.allergies`/`.special_instructions`, already present in `packages/canonical-types/src/tools.ts` — GAP_REGISTER.md §2 Restaurant item 2). Same drift pattern as the `room_type` request above: the runtime schema already accepts these fields, but the model has no declared slot to fill them from since the tool definition it sees doesn't list them. `packages/templates/src/verticals/restaurant.ts`'s `confirm_order` state (this pass, Cluster F) already instructs passing both, ready for when this lands — see that file's own top-of-file NOTE.
  **Requesting cluster:** F (per-vertical template authoring) — blocking for the restaurant vertical's mandatory allergy ask to actually reach the `orders` table, not just the transcript.

- **Target:** `packages/canonical-types/src/tools.ts`'s `zTakeMessageRequest`, plus the mirrored runtime schema in `supabase/functions/_shared/schemas/voice-tools.ts` (neither owned by Cluster F).
  **Change:** add an optional `structured_payload` field to `take_message`, reusing the SAME per-vertical typed shapes `create_booking` already has (`zBookingStructuredPayloadFor(vertical)` / `zBookingStructuredPayload` union, `packages/canonical-types/src/booking-payloads.ts` — `zLegalBookingPayload` already declares exactly `matter_type`/`opposing_party`/`conflict_check_cleared`/`referral_source`/`urgency`, and `zRealEstateBookingPayload` already declares `buyer_or_seller`/`area`/`pre_approved`/`timeline`/`budget_cents`, apparently anticipating this). GAP_REGISTER.md §2 Legal item 4: today, a caller who leaves a message instead of completing a booking (legal has no booking concept at all; real_estate's "lead, not ready to schedule a showing" path) has their qualified/intake data land only in `take_message`'s free-text `message_text`, with no typed column the way a completed `create_booking` gets. Until this lands, `packages/templates/src/verticals/legal.ts` and `real-estate.ts` (this pass, Cluster F) work around it by instructing the model to compose `message_text` as fixed, labeled lines (see each file's own `STRUCTURED_..._CAPTURE_FRAGMENT` constant) — recoverable by a human reading the message, but not queryable/filterable the way a real column would be.
  **Requesting cluster:** F (per-vertical template authoring) — not blocking (the labeled-text fallback is functional today), but closes a real gap in how much of this data the dashboard can ever surface.

- **Target:** `supabase/functions/voice-inbound/dynamic-variables.ts` (`resolveVerticalDynamicVariables`, restaurant branch) + `packages/canonical-types/src/agent-template.ts`'s `zRestaurantOverrides` (neither owned by Cluster F).
  **Change:** add `prep_time_minutes` (already a field on `zBaseDynamicVariableOverrides`, just never surfaced as a spoken `{{prep_time_text}}`-style token for any vertical) and a new `delivery_fee_cents`/`min_order_cents`-derived `{{delivery_terms_text}}` token (`zRestaurantOverrides` already has `min_order_cents`; `delivery_fee_cents` would be a new field) to restaurant's resolved dynamic variables, formatted the same way `resolveMotelTokens`/`resolveLegalTokens` format money into speakable text. This closes GAP_REGISTER.md §2 Restaurant's "order-ready expectation" and "delivery fee/minimum statements" items (both in this pass's Cluster F task brief). Deliberately NOT wired into `restaurant.ts`'s prompt yet — referencing an unresolved `{{token}}` before the backend resolves it would recreate GAP_REGISTER.md §1.3's exact "literal placeholder spoken to the caller" failure mode. `restaurant.ts` is ready to add the fragment the moment this lands.
  **Requesting cluster:** F (per-vertical template authoring) — not blocking anything already shipped; closes two named-but-unimplemented register items.

- **Target:** `supabase/functions/voice-tools/tools/` (a new `list_offerings`-style read-only tool, or a tenant-configured static offering/appointment-type mapping surfaced as a dynamic variable) + `packages/templates/src/shared/tools.ts` (the corresponding `CanonicalTool` builder) — none owned by Cluster F.
  **Change:** GAP_REGISTER.md §2 Dental item 3 and Vet item 4 both need a real, tool-backed (never model-invented) way to resolve an appointment-type/offering to `offering_id` before `check_availability`/`create_booking` — dental's `new_or_existing`/`pain_triage` states and vet's `symptom_or_routine` state currently can only pass a free-text `reason_for_visit`/`visit_reason` into `structured_payload` (this pass, Cluster F, already wired) with no way to also populate `offering_id`, so `bookings.offering_id` stays null and every visit type books identical-duration slots. Needs a real tool or dynamic-variable catalog (mirroring restaurant's `{{menu_text}}` pattern) before a template can wire it without inventing an id — explicitly NOT added by this pass per CLAUDE.md Rule 1 ("never invent"/never guess a catalog).
  **Requesting cluster:** F (per-vertical template authoring) — blocking for `bookings.offering_id` to ever be populated on dental/vet bookings; not a regression (previously nothing populated it either).

- **RESOLVED THIS PASS (Cluster CD, 2026-09-10):** the two Cluster-F-filed requests directly above this note — `checkAvailabilityTool()`'s missing `room_type` property and `createOrderTool()`'s missing `allergies`/`special_instructions` properties (both in `packages/templates/src/shared/tools.ts`) — are done; `check_availability.ts`'s SQL now actually filters on `room_type`/`party_size` too (new `resources.room_type` column, `20260910121000_resources_room_type.sql`), and `create_order.ts` persists `allergies`/`special_instructions` onto new `orders` columns (`20260910120000_orders_delivery_allergies_columns.sql`). Left the two bullets above in place rather than deleting them (not this pass's role per this file's own header — "the integrator applies these"); see `docs/BUILD_NOTES.md`'s Cluster CD entry for the full list of what landed.

- **Target:** `supabase/functions/_shared/templates.ts`'s `TemplateKey` union + `renderTemplate()` switch (not owned by Cluster CD).
  **Change:** add two missing `template_key` cases: `'order_ready'` (now inserted into `messages_outbound` by `apps/web/src/app/api/tenant/orders/[id]/route.ts` on a transition into `status: 'ready'`, GAP_REGISTER.md §2 Restaurant item 6 — e.g. `{ body: "Your order is ready for pickup!" }`) and `'waitlist_slot_opened'` (already inserted by the pre-existing DB trigger `fn_notify_waitlist_on_cancellation`, `20260907131400_functions_triggers.sql`, with `payload: {start, waitlist_entry_id}` — this was a dead/unreachable case before this pass since nothing ever populated `waitlist_entries`; the new `join_waitlist` tool, GAP_REGISTER.md §1.2, now makes it reachable). Without these cases, `renderTemplate`'s `default:` branch returns `{ body: "" }` — both messages currently send as decorative empty-body SMS the instant a live tenant hits either path.
  **Requesting cluster:** whoever owns `supabase/functions/_shared/templates.ts` (Cluster E per its file header's own "the messages_outbound queue worker" framing) — blocking for either notification to actually say anything to the customer, not a regression (both paths are net-new/newly-reachable this pass).

- **Target:** `packages/canonical-types/src/agent-template.ts`'s `zRestaurantOverrides` + `packages/canonical-types/src/schemas/vertical-details.ts`'s `verticalDetailsSchema` (neither owned by Cluster CD).
  **Change:** add a `delivery_fee_cents` field (optional, `zCents`) to both schemas, mirroring the existing `min_order_cents`/`delivery_radius_m`/`tax_rate_bps` fields' shape and Settings-UI presence — `create_order.ts` (this pass, Cluster CD, GAP_REGISTER.md §4 "delivery_fee_cents ... enforced from per-vertical config") already reads `agent_configs.dynamic_variable_overrides.delivery_fee_cents` directly and applies it to `orders.delivery_fee_cents`/`total_cents` for a delivery order, same pattern `tax_rate_bps` used before its own Settings-UI field existed (Cluster B's earlier pass) — but there is currently no tenant-facing way to SET this value at all (no field in `apps/web`'s vertical-details Settings tab), so it silently defaults to 0 for every restaurant tenant today. Not a regression (delivery fees were never configurable before this pass either); the backend enforcement path is real and tested, just missing its config UI entry point.
  **Requesting cluster:** A/B (canonical-types `z*Overrides` + `vertical-details.ts` + the Settings UI vertical-details page) — non-blocking, current behavior (fee defaults to $0) is safe, not decorative.

- **Target:** `packages/templates/src/verticals/*.ts` (7 files: auto-repair, dental, generic, motel, real-estate, restaurant, veterinary) — not owned by Cluster CD.
  **Change:** `createBookingTool(description)` and `takeMessageTool()` in `packages/templates/src/shared/tools.ts` (Cluster CD's file) now accept an optional second `vertical: Vertical` argument that surfaces that vertical's typed `structured_payload` JSON-Schema `properties` (`BOOKING_STRUCTURED_PAYLOAD_PROPERTIES`, `packages/canonical-types/src/booking-payloads.ts`, GAP_REGISTER.md §1.7) to the model as an authoring hint instead of a bare `{type:"object"}` — Retell surfaces tool JSON-Schema `properties` to the LLM, which measurably improves fill rate. Left optional (every existing call site still compiles/passes unchanged, confirmed by `pnpm --filter @heyloo/templates test`/`typecheck`, both green) rather than edited directly, since `verticals/*.ts` is Cluster F's exclusive ownership. Change each vertical's own `createBookingTool("...", "auto")` / `createBookingTool("...", "motel")` / etc. call (and `takeMessageTool("legal")`/`takeMessageTool("real_estate")` for the two verticals using the message-fallback structured-capture workaround noted elsewhere in this file) to pass its own vertical slug — a one-argument change per file.
  **Requesting cluster:** F (per-vertical template authoring) — non-blocking (the bare-object shape still validates fine server-side), pure model-authoring-quality improvement.

- ~~**Target:** `supabase/functions/api-intake/` (new edge function) + a new additive migration for wherever intake tokens/DOB/insurance land~~ — **APPLIED** (this pass, Cluster G): built exactly against this contract — `GET/POST /functions/v1/api-intake/{token}` (token carried as the remaining URL path segment, `index.ts` strips the function-name prefix same as `admin/index.ts`), `GET` → `200 {valid,tenant_name,patient_first_name,already_submitted}` or `404`, `POST` body `{date_of_birth, insurance_provider?, insurance_member_id?, insurance_group_id?}` → always HTTP 200 with `{ok:true}` or `{ok:false, error:"expired"|"already_submitted"|"invalid"}` (never a non-2xx, so `functions.invoke()` doesn't throw). New tables `intake_tokens` (opaque single-use token, only its sha256 hash stored, `patient_first_name` snapshotted at issuance) / `intake_submissions` (DOB/insurance AES-256-GCM-encrypted via a dedicated `INTAKE_ENCRYPTION_KEY`) — `20260910140300_dental_intake.sql`; token issuance + the `dental_intake_link` SMS template landed in `_shared/dental-intake.ts`/`_shared/templates.ts` (`{APP_BASE_URL}/intake/{token}`, matching this entry's own suggested link shape) — its call site into `create_booking.ts` is filed as a new FIX_REQUESTS entry below (still needs Cluster CD to wire the one call). Test coverage: `pnpm --filter @heyloo/edge-functions test` (api-intake/handler.test.ts, _shared/dental-intake.test.ts, _shared/templates.test.ts), full suite green; migrations verified applying cleanly from zero.

- ~~**Target:** `supabase/functions/api-menu-import/` (new edge function)~~ — **APPLIED** (this pass, Cluster G): `raw_text` is the primary/required input path (matches the already-built proxy exactly — no `tenant_id` in the body, JWT-derived tenant membership check instead); a richer additive `source: {kind:"url"|"file", ...}` mode (Anthropic vision, PDF/photo/URL — this task's original brief) is also accepted for a future upgraded client, but never required. Response is `{items: [...]}` with exactly the `name/category?/price_cents?/duration_minutes?/allergens?/modifiers?` shape this entry specifies (`_shared/schemas/menu-import.ts`'s `MenuImportCandidateSchema`). Test coverage: `pnpm --filter @heyloo/edge-functions test` (api-menu-import/handler.test.ts), full suite green.

- **Target:** `supabase/functions/voice-tools/tools/create_booking.ts` (Cluster CD's file — not owned by Cluster G).
  **Change:** the dental-intake backend above is fully built and ready (`_shared/dental-intake.ts`'s `issueDentalIntakeToken(sql, {tenantId, bookingId, customerPhoneE164, customerName?}, {appBaseUrl})` — a single call after a booking insert succeeds, tests already cover it) but nothing calls it yet. Add, after a successful `create_booking` insert when the tenant's `vertical === 'dental'`: `await issueDentalIntakeToken(sql, {tenantId, bookingId: booking.id, customerPhoneE164: customer.phone_e164, customerName: customer.name}, {appBaseUrl: DENTAL_INTAKE_APP_BASE_URL})` (a new `APP_BASE_URL`-sourced env read in that function's own `index.ts`, matching how every other URL-building call site in this codebase reads it). Never throws/blocks the booking itself on failure — wrap in try/catch and log, same fail-open posture as any other post-booking side effect (reminders, review requests).
  **Requesting cluster:** G (this pass) — blocking for the dental-intake feature to actually reach a real booking; both endpoints and the DB schema already work standalone and are fully tested.

- **Target:** `packages/supabase-client/src/database.types.ts` — not owned by Cluster E.
  **Change:** informational, non-blocking. `BookingRow` is missing `quoted_rate_cents`/`hold_expires_at` (real columns, `20260910120500_bookings_quoted_rate_and_hold_expiry.sql`), `OrderRow` is missing `allergies`/`special_instructions`/`delivery_fee_cents` (real columns, `20260910120000_orders_delivery_allergies_columns.sql`), and `ResourceRow` is missing `room_type` (real column, `20260910121000_resources_room_type.sql`). Confirmed empirically (a temporary probe file + `pnpm --filter @heyloo/web typecheck`) that an unrecognized column in a `.select()`/`.insert()` string does NOT error at the call site — it only degrades the query's inferred result type to `SelectQueryError<...>`, which errors solely if a property is then read off an uncast result — so `apps/web`'s dashboard-surfacing pages (`dashboard/bookings/page.tsx`, `dashboard/orders/[id]/page.tsx`, `dashboard/setup/resources/page.tsx`) and the new `api/tenant/resources/**` routes already read/write these live columns today via an immediate cast-after-query (documented inline at each call site) rather than waiting on this. Adding the fields to the hand-maintained Row types would let those call sites drop the casts and get real compile-time checking back.
  **Requesting cluster:** whichever cluster owns `packages/supabase-client` (Cluster D, per BUILD_PLAN's DB-fields/typed-payloads ownership) — informational only, nothing shipped by Cluster E is blocked on it.

- **Target:** a NEW `supabase/functions/api-tenant-test-call/` edge function (not owned by Cluster H — only `apps/web/**` is).
  **Change:** Cluster H's test-your-agent page (`dashboard/test-agent/**`, task brief item 2) needs a "start a web call" action for an already-provisioned, already-published tenant agent. `apps/web/src/app/api/tenant/test-agent/web-call/route.ts` (this cluster's own file, built and tested) already proxies `POST` to `{SUPABASE_URL}/functions/v1/api-tenant-test-call` with the caller's own bearer token — that function does not exist yet, so the proxy currently 503s and the page shows an honest "not available yet" message (same degrade pattern as the pre-existing `api-menu-import`/`api-intake` requests above) rather than a fake success. Suggested contract: `POST` (authenticated tenant session, `tenant_id` from the JWT only, never a body param) → resolve the tenant's `agent_configs.retell_agent_id` (404 `{error: "agent_not_published"}` if unset) → call Retell's create-web-call endpoint (`packages/adapters/retell`'s existing `createWebCall`-shaped helper in `supabase/functions/_shared/providers/retell.ts` already has this for the pre-signup demo flow, `api-demo-agent/handler.ts` — reuse the same helper, just against the tenant's real published agent instead of a demo template) → insert the resulting `call_logs` row (or let `voice-events`'s existing webhook path do it) with `is_test_call: true` regardless of `owner_test_phone` matching, since a web call has no caller PSTN number to match against → `200 {access_token: string, call_id: string}`. Provider isolation (CLAUDE.md Rule 2) is the reason this can't be built from `apps/web` directly.
  **Requesting cluster:** whichever cluster owns `supabase/functions/**`'s voice/conversation surface (the same cluster that owns `api-demo-agent`) — blocking for the web-call half of test-your-agent; the phone-call half (call your own provisioned number) works today with no new backend.

  **Resolved (repair pass):** the edge function now exists (`supabase/functions/api-tenant-test-call/{index,handler}.ts`) and returns exactly the contract above (`200 {access_token, call_id}`, `404 {error: "agent_not_published"}`, `502 {error: "call_token_unavailable"}`); `apps/web/src/app/api/tenant/test-agent/web-call/route.ts`'s proxy and doc comment were already correct/updated. The one remaining gap was on this cluster's own side: `test-agent-client.tsx`'s `startWebCall()` fetched the route but never read the response body, so the returned token was discarded and no audio session was ever established. Fixed by parsing `{access_token}` and calling `RetellWebClient.startCall({accessToken})` (lazy-imported `retell-client-js-sdk`, mirroring `components/demo/demo-flow.tsx`'s `ActiveStep`), with call-lifecycle UI state (connecting/active/ended/error) mirroring that same reference. Covered by a new success-path test in `test-agent-client.test.tsx` (mocks `retell-client-js-sdk`, asserts `startCall` is invoked with the backend's token, and exercises the `call_started`/`call_ended` lifecycle events). No longer blocking; item closed.

- **Target:** `packages/supabase-client/src/database.types.ts` (not owned by Cluster H).
  **Change:** two real correctness gaps found while building the partner portal breakdown/payouts pages (Cluster H task brief item 4), both worked around locally with an explicit cast per the established pattern (see other entries above), but both are worth fixing at the source: (1) `ReferralPayoutRow` claims columns `amount_cents`/`method`/`paid_at` that do not exist on `referral_payouts` at all (the real columns, per `20260907130800_referrals.sql`, are `total_cents`/`period`/`status`, and `status`'s real CHECK constraint per `20260910100400_referral_payouts_terminal_statuses.sql` is `'pending'|'sent'|'completed'|'failed'|'returned'`, not the type's `'pending'|'processing'|'paid'|'failed'`) — this is not a missing-field gap like the others in this file, the type is actively wrong, and `apps/web/src/app/[locale]/(partner)/portal/payouts/page.tsx` previously selected the nonexistent columns (a real bug, fixed this pass to select the real ones with a cast). (2) `ReferralPartnerRow` is missing `rate_bps`/`commission_base`/`duration_months` (`20260910140000_referral_commission_recurring.sql`), and the `commission_events`/`referral_partner_vertical_overrides` tables aren't in the `Database` type's table union at all — every route/page this cluster wrote against them (`api/admin/admin-referral-partners/**`, `portal/customers/page.tsx`) casts `.from` itself via a small `fromUntypedTable` helper (`apps/web/src/app/api/admin/_lib/admin-auth.ts`) rather than per-column casts, since the whole table is unknown to the type, not just some columns.
  **Requesting cluster:** whichever cluster owns `packages/supabase-client` (Cluster D) — non-blocking (every call site already works via the documented cast workaround), but the `ReferralPayoutRow` mismatch in particular is a trap for the next person who trusts the type instead of the real schema.

- **Target:** `supabase/functions/admin/handler.ts`'s `NOT_YET_IMPLEMENTED_PREFIXES` list and `handlePlatformSettings`/`handleReferrals` (not owned by Cluster H).
  **Change:** informational/consolidation request, non-blocking. Cluster H needed an admin ticket queue (task brief item 3), per-partner commission-terms editor (item 4), and a setup-fee/white-glove settings tab (item 5), and that edge function either 501s the first (`admin-support-requests` is literally in `NOT_YET_IMPLEMENTED_PREFIXES`) or has no route at all for the second two. Rather than block on it, this cluster implemented all three as direct-to-Postgres Next.js Route Handlers under `apps/web/src/app/api/admin/{admin-support-requests,admin-referral-partners,admin-platform-settings/fees}/**` (service-role client, `platform_admin` claim check matching `api/admin/[...path]/route.ts`'s own gate, `admin_actions` audit writes matching `writeAdminAction`'s shape) — real, tested, working today, not stubs. Next.js resolves a literal route (e.g. `api/admin/admin-support-requests/route.ts`) ahead of the `[...path]` catch-all, so these coexist with the proxy with no routing conflict. Whoever owns the edge function may want to fold this logic into it later for architectural consistency with the rest of `/api/admin/*` (which all proxy through one function) — not required for correctness, since these routes already enforce the same security bar independently.
  **Requesting cluster:** whichever cluster owns `supabase/functions/admin/**` — informational, no action required unless consolidation is wanted.
  **Acknowledged (this pass, Cluster G):** independently implemented the edge-function side of the same three surfaces (`admin-support-requests` list/get/PATCH/notes, `admin-referrals/partners/:id` PATCH commission-terms + `/vertical-overrides/:vertical` PUT — `NOT_YET_IMPLEMENTED_PREFIXES` no longer lists `admin-support-requests`) as part of this pass's own task brief, unaware of Cluster H's parallel Route Handlers at the time. Confirmed no runtime conflict (Next.js's literal-route-before-catch-all resolution, per this entry's own note, means neither path is ever reached by the other) and left both in place per this entry's own "not required for correctness" framing — consolidating onto one is a future cleanup, not attempted here. Did NOT touch `admin-platform-settings/fees` (Cluster H's `fees_<vertical>` key/shape) — the setup-fee/white-glove item in this pass's own brief instead wired the consuming side (`api-checkout`) to read that exact same `fees_<vertical>` key once this contract was found, rather than building a second, conflicting settings surface.

- **Target:** a team-management/invite UI (a new `dashboard/settings` or `dashboard/team` page + a `POST`-capable invite endpoint) — not owned by Cluster H (no such screen exists anywhere in `apps/web` today, and building one is outside this cluster's onboarding/testing/growth-UI brief).
  **Change:** the setup-progress panel (task brief item 1) computes a real "Invite your team" step from `memberships` count (`> 1` = done), since that's genuinely queryable today — but there is nowhere in the whole app to actually invite anyone (`memberships.user_id` is `not null`, so a row can't even be created for someone without an auth user yet; inviting requires `supabase.auth.admin.inviteUserByEmail` from a service-role context, i.e. a new edge function or Route Handler, plus a page to drive it). The panel's step therefore has `href: null` (no dead link) until this exists.
  **Requesting cluster:** whichever cluster owns tenant account/team settings (unclear — no existing owner found; flagging for the integrator to route to the right cluster or a future wave).

- **Target:** `tenants.policies_reviewed_at timestamptz` (new, additive migration) — not owned by Cluster H (`supabase/migrations/**` isn't in this cluster's file list).
  **Change:** the setup-progress panel's "Review your cancellation & booking policy" step (task brief item 1) is computed from the closest real existing signal — `agent_configs.dynamic_variable_overrides.cancellation_policy.text` being non-empty (a real, universally-collected field per `vertical-details.ts`'s form, confirmed non-optional across every vertical) — rather than a dedicated "reviewed" flag, since no such column exists and this cluster cannot add migrations. That proxy is honest (a tenant can't have a cancellation policy configured without having seen the vertical-details form that states it) but conflates "configured" with "reviewed." A real `tenants.policies_reviewed_at` column, set by an explicit "I've reviewed this" acknowledgment action somewhere in Settings, would be more precise.
  **Requesting cluster:** whichever cluster owns `supabase/migrations/**` + the vertical-details Settings page — non-blocking, current proxy signal is real and not fabricated.

- **Target:** a new `job-lead-callback-retry` (or similarly-named) scheduled function — not in Cluster G's granted ownership for this pass (only `api-menu-import`/`api-intake-submit`(now `api-intake`)/`api-lead-callback`/`job-commission-accrual` were new-function-directory grants).
  **Change:** `api-lead-callback` (GAP_REGISTER Cluster G item 2) correctly refuses to place an outbound call during the tenant's quiet hours — it writes the `lead_callback_requests` row as `status: 'deferred_quiet_hours'` with a computed `scheduled_for` (next quiet-hours-end, tenant-local) rather than calling anyway or silently dropping the lead — but nothing currently drains that status once quiet hours end; a deferred lead stays `deferred_quiet_hours` forever without a follow-up job. A small cron job (mirroring `job-reminder-scheduler`'s own quiet-hours-aware pattern) that selects `lead_callback_requests where status = 'deferred_quiet_hours' and scheduled_for <= now()` and re-runs the same outbound-call logic (`handleLeadCallback`'s call-placing branch, refactored into a directly-callable function if useful) would close this. Not attempted here since it needs a new top-level function directory outside this pass's ownership grant.
  **Requesting cluster:** whichever cluster owns scheduled jobs / can add a new `supabase/functions/job-*` directory — non-blocking for the immediate-call happy path (the overwhelming majority of daytime submissions), but a deferred lead currently never gets called back automatically.
  **Resolved (WAVE-2 build wave, prior to the integration pass):** `supabase/functions/job-lead-callback-retry/` now exists and is cron-scheduled (`supabase/migrations/20260910160000_wave2_cron.sql`, `supabase/config.toml`). Noted here by the WAVE-2 integration pass only for FIX_REQUESTS bookkeeping accuracy — not that pass's own work.

- **Target:** root `turbo.json` — not owned by the repair cluster that closed the vet transfer_call bug (only `packages/canonical-types/**`, `packages/templates/**`, `packages/adapters/retell/**` were in scope).
  **Change:** `packages/adapters/retell/src/compiler/registry-consistency.test.ts` now reads `packages/templates/dist/templates.build.json` (a plain filesystem read, no package.json dependency edge — deliberately, to avoid recreating the circular dependency the file's own INTEGRATION NOTE documents) so its transfer_call-native-wiring / token-schema-consistency / single-tool-node-locking checks run against the real 8 shipped templates, not just synthetic fixtures. `turbo.json`'s `test` task's default `dependsOn: ["^build"]` only orders a package's OWN declared dependencies' build tasks first — since `@heyloo/adapter-retell` does not (and must not) declare `@heyloo/templates` as a package.json dependency, turbo's graph does not guarantee `@heyloo/templates#build` runs before `@heyloo/adapter-retell#test` in a from-scratch `pnpm run test` (e.g. CI's `test` job, which does a fresh checkout with no prior `pnpm run build` step per `.github/workflows/ci.yml`). Right now the loader (`loadRealRegistry()`) throws a clear, actionable error rather than silently skipping if the artifact is missing — so this fails loudly instead of quietly losing coverage, but it will fail the whole `@heyloo/adapter-retell` test suite in a from-scratch CI `test` job until fixed. Suggested fix: add a package-specific task override to `turbo.json` — `"tasks": {"@heyloo/adapter-retell#test": {"dependsOn": ["@heyloo/templates#build"]}}` (Turborepo supports task-level `<package>#<task>` anchoring independent of the package.json dependency graph, so this does not recreate the cycle) — or add an explicit `pnpm --filter @heyloo/templates build` step before the `test` job in `.github/workflows/ci.yml`.
  **Requesting cluster:** REPAIR (vet transfer_call / registry-consistency fix, this pass) — blocking for `registry-consistency.test.ts`'s real-registry coverage to survive a from-scratch CI run; not blocking today since `packages/templates` happens to already be built in this working tree.
  **Resolved (WAVE-2 integration pass):** added exactly the suggested package-specific task override to root `turbo.json` — `"@heyloo/adapter-retell#test": {"dependsOn": ["^build", "@heyloo/templates#build"], "outputs": []}` (the full task config had to be repeated, not just the new dependency — confirmed via Turborepo's own docs this pass: a `pkg#task` entry in root `turbo.json` fully replaces the general task's config for that package, it does not merge with `$TURBO_EXTENDS$`, which is Package-Configuration-file-only). Verified with `turbo run test --filter=@heyloo/adapter-retell --dry=json`: `@heyloo/adapter-retell#test`'s `dependencies` now lists `@heyloo/templates#build`. `pnpm -w test` (and `pnpm --filter @heyloo/adapter-retell test` from a clean tree) now builds `@heyloo/templates` first and the real-registry checks in `registry-consistency.test.ts` execute for real (38 tests, including against the actual 8 shipped templates) rather than only when a human happened to build in the right order first. Item closed.

- **Target:** `supabase/functions/_shared/schemas/menu-import.ts`'s doc comment (not owned by this repair pass — only `apps/web/**` was in scope for the menu-import gap).
  **Change:** the comment above `MenuImportRequestSchema` (and the mirrored note in `docs/BUILD_NOTES.md`'s Cluster G entry) states that `raw_text` is "the REQUIRED, already-wired primary path" and that `source` (url/file vision) is "additive... for when the client is upgraded to send an actual PDF/photo." That is no longer accurate: this repair pass wired the real client. `apps/web/src/app/api/tenant/offerings/import/route.ts`'s `importRequestSchema` now accepts the same discriminated `source: {kind:"url"|"file", ...}` shape (mirrored from this schema) and forwards the body unchanged, and `apps/web/src/app/[locale]/(tenant)/dashboard/setup/offerings/import/page.tsx` now has a real "Import from URL" input plus a file branch that base64-encodes `image/*`/`application/pdf` uploads (`FileReader.readAsDataURL`) and posts `{source:{kind:"file",...}}` instead of force-reading every upload as `file.text()`. Both `source:url` and `source:file` are exercised end-to-end today, not just accepted-and-unused. No code change requested — purely so the next reader of this file's comment (or BUILD_NOTES) doesn't re-conclude the gap still exists.
  **Requesting cluster:** whichever cluster owns `supabase/functions/**` (Cluster G) — informational only, non-blocking.

- **Target:** `packages/adapters/retell` (new file, e.g. `src/tests-api.ts`, exported from `src/index.ts` alongside `RetellProvider`) — NOT owned by this repair pass (CLAUDE.md Rule 2: provider SDK usage/provider-shaped payloads stay inside `packages/adapters/*`, and this file path is outside the red-team/batch-simulation cluster's ownership).
  **Change:** add `createRetellBatchSimulationClient(opts: {apiKey: string}): BatchSimulationClient`, where `BatchSimulationClient` is the provider-agnostic interface already defined and consumed at `packages/templates/src/red-team/simulation-types.ts` (`runScenarios(templateKey, responseEngineRef, cases): Promise<ReadonlyMap<string, SimulationTranscript>>`). The batch-simulation harness (`packages/templates/src/red-team/run-simulation.ts`) is fully built and unit-tested against a mock implementation of this interface (`run-simulation.test.ts`) — this is the ONE remaining piece: a real implementation wrapping Retell's own `client.tests` resource (`createTestCaseDefinition`/`createBatchTest`/`getTestRun`/`listTestRuns` — shape confirmed via the official `retell-sdk` npm package's generated source, `node_modules/retell-sdk@5.64.0/resources/tests.d.ts`, logged as VERIFY-13 in `docs/VERIFY.md`). Implementation sketch: for each template's `responseEngineRef` (an opaque `{conversation_flow_id|llm_id, version}` string this package's caller resolves from `createOrUpdateRetellAgent`/`publishRetellAgentVersion` against a STAGING-only Retell account, SYSTEM_DESIGN §8 G16 — never prod), (1) `createTestCaseDefinition` once per `SimulationTestCase` (`user_prompt: testCase.personaPrompt`, a placeholder non-empty `metrics: []` or a sensible default), (2) `createBatchTest` with all the resulting definition ids, (3) poll `getTestRun`/`listTestRuns` until every job leaves `pending`/`in_progress`, (4) normalize each `TestCaseJobResponse.transcript_snapshot` (typed `unknown` by the SDK itself — VERIFY-13 flags this as not yet independently confirmed against a live account) into this package's canonical `SimulationTranscript` (`reachedStates`, `toolCalls: {name, arguments}[]`, `firstAgentUtterance`, `agentUtterances`) behind a runtime Zod validator at that exact boundary (CLAUDE.md Rule 1) — a live Retell staging call is needed first to see one real `transcript_snapshot` and confirm its field names.
  **Requesting cluster:** REPAIR (batch-simulation harness pass, this pass) — blocking for the harness's live leg (`pnpm --filter @heyloo/templates run simulate`) to ever produce a real result; the harness's own plumbing/grading logic is already real and green without it (`pnpm --filter @heyloo/templates test`).
  **Resolved (WAVE-2 integration pass), partially:** added `packages/adapters/retell/src/tests-api.ts`, exported from `src/index.ts` — `createRetellBatchSimulationClient(opts: {apiKey, baseUrl?, pollIntervalMs?, pollTimeoutMs?, fetchImpl?, sleep?})` returning a real `BatchSimulationClient`. Uses this package's existing hand-rolled `RetellClient` (fetch-based), not the `retell-sdk` package itself (matches this package's own established convention — `retell-sdk` stays a devDependency used only for compile-time shape checks, `sdk-contract.test.ts`). Implements exactly the sketch above: one `create-test-case-definition` per case (`metrics` filled with a fixed, harmless placeholder string since this harness grades transcripts itself and never relies on Retell's own LLM-judged verdict), one `create-batch-test`, then polls `GET /v2/list-test-runs/{id}` until every submitted case leaves `pending`/`in_progress` (bounded by a `pollTimeoutMs`, default 10 minutes — throws rather than hanging forever) or errors (throws immediately, never fabricates a pass). `BatchSimulationClient`/`SimulationTestCase`/`SimulationTranscript`/`RecordedToolCall` are a hand-kept STRUCTURAL duplicate of `simulation-types.ts`'s identically-named exports, not an import — importing `@heyloo/templates` from here would recreate the exact circular package dependency this cluster's own `registry-consistency.test.ts` documents avoiding in the other direction; `run-simulation.ts`'s `loadRealClient` already dynamic-`import()`s + casts, so no import edge is needed for either side to type-check. Unit-tested (`tests-api.test.ts`, 6 cases: empty input, the full submit→poll→normalize happy path, an errored case, an unrecognized `transcript_snapshot` shape, a malformed `responseEngineRef`, and a poll timeout) with an injected `fetchImpl`/`sleep`, no live account needed.
  **NOT fully resolved — the one piece genuinely requiring a live account, per VERIFY-13's own standing note:** `transcript_snapshot`'s internal field names are still unconfirmed by any reachable documentation (the SDK types the field `unknown` on purpose). `normalizeTranscriptSnapshot` is written against the closest officially-documented sibling shape this same SDK uses for the same concept elsewhere (the `Call` resource's `transcript_with_tool_calls` discriminated union) behind a Zod boundary that throws a loud, specific, actionable error — never a silently-empty transcript — the moment a real payload doesn't match one of a few candidate shapes. `docs/VERIFY.md` VERIFY-13 updated accordingly; still logged as open pending a live Retell staging run. `.github/workflows/ci.yml`'s wiring request below is now technically unblocked (both prerequisite items in this file are closed) but wiring the actual CI job was left for whoever owns `.github/workflows/**`, per that entry's own scope note — not attempted here (CLAUDE.md Rule 4: out of this integration pass's assigned scope).

- **Target:** `.github/workflows/ci.yml` — NOT owned by this repair pass (shared CI config, outside `packages/templates/**`).
  **Change:** add a job gated on changes to `packages/templates/src/verticals/**` and `packages/templates/src/shared/**` (the same path-filter approach this directory's `README.md` already sketched before this pass) that runs `pnpm --filter @heyloo/templates build && pnpm --filter @heyloo/templates run simulate`, conditioned on a `RETELL_STAGING_API_KEY`-shaped secret being present (e.g. `if: ${{ secrets.RETELL_STAGING_API_KEY != '' }}`) so the job is skipped, not red, until a staging account + the adapter wrapper above both exist — the script itself already fails closed (non-zero exit, no fabricated pass) whenever `RETELL_API_KEY` is unset or the adapter export is missing, so this job should NOT be marked required/blocking until then. The harness's own unit tests (`pnpm --filter @heyloo/templates test`, no secret needed) should already be running in the normal test job and need no new wiring.
  **Requesting cluster:** whichever cluster owns `.github/workflows/**` — non-blocking until the two items above land.
  **Update (WAVE-2 integration pass):** both prerequisite items above are now resolved (`turbo.json` task override landed; `createRetellBatchSimulationClient` landed in `packages/adapters/retell/src/tests-api.ts`) — this workflow-wiring request is now actionable, still not attempted here (`.github/workflows/**` outside this pass's assigned scope, CLAUDE.md Rule 4). Still requires a real `RETELL_STAGING_API_KEY`-shaped secret (name only, no value) before the job can do anything beyond fail closed.
