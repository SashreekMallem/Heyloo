/**
 * @heyloo/ui — placeholder.
 *
 * Shared component library for the tenant dashboard, admin cockpit, and
 * partner portal. Real components land in T5 once the frontend stack
 * decision doc merges (SYSTEM_DESIGN §3 leaves framework choice open;
 * apps/README.md tracks the pending decision). This file only proves the
 * package builds and is importable.
 */

export const UI_PACKAGE_VERSION = "0.0.0" as const;
