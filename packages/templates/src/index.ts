/**
 * @heyloo/templates — placeholder.
 *
 * The 8 vertical agent templates (auto, veterinary, legal, dental, real
 * estate, motel, restaurant, generic) in canonical conversation-spec format
 * — SYSTEM_DESIGN §4.1–§4.3. Real templates, the red-team adversarial test
 * suite, and the batch-simulation CI harness land in T6. This file only
 * proves the package builds and is importable.
 */

export const TEMPLATES_PACKAGE_VERSION = "0.0.0" as const;
