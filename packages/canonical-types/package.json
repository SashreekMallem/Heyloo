{
  "name": "@heyloo/canonical-types",
  "version": "0.0.0",
  "private": true,
  "description": "Provider-agnostic types shared across Heyloo: voice/agent schema, tenant domain model, tool contracts. Nothing here may import a provider SDK — see packages/adapters/README.md.",
  "type": "module",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "5.0.0"
  }
}
